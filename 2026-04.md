# 2026-04

## 2026-04-29

- `git revert`의 효과
  - 왜 그런 일이 생기게 되었는지 모르겠으나 코드 릴리즈를 준비하며 기능들을 쌓고 있었는데 갑자기 그 중 일부 커밋은 제외한 나머지를 main 브랜치에 반영해 패치를 해야 하는 상황이 생겨버렸다
  - 그래서 패치 담당자가 revert를 어찌저찌 활용하여 Reapply 커밋까지 만들어버렸는데
  - 문제는 그렇게 큰소리 땅땅 치던 내가 Git revert를 아직 제대로 이해를 못 해서 일단 "아 revert로 이런 것까지 되는 구나"는 알았고 어떻게 그런 일이 가능했는지는 분석을 해봐야 할 것 같다

## 2026-04-30

- `git revert`의 효과 (2)
  - 답답해서 Pro Git에서 찾아봤는데 너무 명쾌하게 설명돼있었다
    > git revert 명령은 git cherry-pick 명령의 반대로 볼 수 있다. 해당 커밋을 되돌리는 커밋을 새로 생성한다.
  - 나는 처음에 `git revert`를 수행하면 되돌리는 커밋을 생성한다길래 revert의 대상이 되는 시점의 커밋부터 최신 커밋까지의 히스토리가 통째로 지워지는 줄 알았다
  - 왜 그런 착각을 하게 됐나 생각해보니 GitHub에서 PR을 merge할 때 새 merge commit이 생성되거나 squash를 하기 때문에 PR로 merge된 커밋을 revert하면 꽤 긴 히스토리가 삭제되는 것이었나? 싶다
