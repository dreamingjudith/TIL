# 2025-10

## 2025-10-22

- (우분투 기준) 인터넷이 안 되는 환경에서 uv를 설치하고 싶을 땐 GitHub release를 다운받은 다음 압축해제를 해서 나오는 `uv`. `uvx` 바이너리를 `$HOME/.local/bin`에 집어넣는다
  - 어떻게 알았냐면 설치하고 싶었던 서버를 걍 억지로 인터넷이 되게 만든 다음 인스톨러를 돌렸더니 이런 메시지가 출력됨
  ```plaintext
  downloading uv 0.9.4 x86_64-unknown-linux-gnu
  no checksums to verify
  installing to /home/judith/.local/bin
    uv
    uvx
  everything's installed!

  To add $HOME/.local/bin to your PATH, either restart your shell or run:

    source $HOME/.local/bin/env (sh, bash, zsh)
    source $HOME/.local/bin/env.fish (fish)
  ```