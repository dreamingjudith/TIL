# 2026-03

## 2026-03-18

- MCP는 JSON-RPC 2.0 규격으로 서버와 클라이언트가 데이터를 주고받는다고 한다 (!!)
- FastMCP의 decorator로 등록한 tool이 리턴하는 값은 FastMCP가 알아서 `ToolResult`로 감싸서 리턴하는데
  - `ToolResult`의 구성요소는 아래의 3개로 이뤄져 있음
    - `content`
    - `structured_data`
    - `meta`
  - 이 때 내가 특별히 따로 정의해주지 않으면 int, str와 같이 단순한 타입이 아닌 오브젝트들은 `content`가 자동으로 `TextContent`로 인식되어 감싸지는 것 같았다
    ```python
    @mcp.tool
    def list_collections() -> list[str]:
        return ["important-documents"]

    # MCP client가 list_collections를 호출한 결과
    CallToolResult(
        content=[TextContent(type="text", text='["important-documents"]')],
        structured_content={"data": ["important-documents"]},
        meta=None,
    )
    ```
  - 그래서 만약에 디폴트로 감싸지는 게 싫으면 셀프로 ToolResult를 정의해서 사용해야 할 것 같은데 일단 FastMCP 문서 상으로는 `TextContent`하고 `ImageContent`만 찾아서 또 뭘 쓸 수 있는지 확인해봐야 할 것 같다
