# 2026-06

## 2026-06-10

-  pre-commit에서 `repo: https://github.com/PyCQA/isort`를 지정한 뒤 버전을 8.0.1로 고정해 돌려보면 정작 실행 결과는 0.1.dev1로 나온다
   - 근데 웃긴 건 나는 8.0.1에서 이런 황당한 현상을 발견했지만 깃허브 이슈를 확인해보니 다른 버전에서도 그런 사례가 있었다는 듯 하다 <https://github.com/PyCQA/isort/issues/2362>
   - 챗지피티와 이 현상에 대해 이야기를 나눴을 때는 이런 의견을 주었다
    ```plaintext
    그리고 작성자는 5.13.2에서는 올바른 버전이 보였다고도 적었습니다. 즉 isort 6.x 이후 특정 변경으로 pre-commit 환경에서 버전 메타데이터 탐지가 깨진 것으로 보이는 문제입니다.
    중요한 건 이 이슈가 Closed as not planned 상태라는 점입니다. GitHub 페이지상 해당 이슈는 “Closed as not planned”로 닫혀 있습니다.
    그래서 정정하면:
    네가 본 VERSION 0.1.dev는 네 설정만의 문제가 아니라, isort가 pre-commit hook 환경에서 자기 metadata/version을 제대로 못 찾는 알려진 현상입니다. 실제 정렬 로직까지 반드시 잘못된 버전으로 동작한다는 뜻은 아닐 수 있지만, 출력상 버전은 신뢰하기 어렵습니다.
    ```

  - 그래서 pre-commit에서 isort의 remote hook 버전이 0.1.dev1 이라 출력된다고 해서 실제 저 버전으로 돌아가는 건 아니라고 결론 내렸다. (실제로도 가상환경으로 돌리던 8.0.1이랑 동작은 별 차이 없어보였음...)
- 그것보다 더 황당(?)했던 건 개발 중인 레포지토리에서 MCP 서버로 띄워야 하는 부분을 편의상 마치 모노레포처럼 pyproject.toml이나 단위 테스트 등을 따로 생성하게끔 리팩토링을 한 이후 발생한 문제였는데
  - 바로 isort가 로컬 모듈을 자꾸 thirdparty로 인식하는 문제였다
  - 이를테면 위의 코드 블록을 자꾸 아래처럼 고치려는 현상이었다
    - 정상 코드
      ```python
      import pytest

      from my_module import my_dict
      ```
    - isort가 자꾸 아래처럼 고치려고 함
      ```python
      import pytest
      from my_module import my_dict
      ```

  - 근데 이것도 사실 isort를 root에서 서브모듈을 제외한 나머지를 돌리고
  - 그리고 서브모듈만 따로 isort를 돌리게끔 설정하면 된다는 것이었다
  - 갓지피티가 그래서 pre-commit을 아래와 같이 설정하고 (이상하다 나한테는 files 대신에 exclude를 쓰는 법을 알려준 거 같은데...)
    ```yaml
    repos:
    - repo: https://github.com/pycqa/isort
      rev: 6.1.0
      hooks:
        - id: isort
          name: isort root
          args: ["--settings-path", "pyproject.toml"]
          files: ^(src|tests)/.*\.py$

        - id: isort
          name: isort my_module
          args: ["--settings-path", "my_module/pyproject.toml"]
          files: ^my_module/.*\.py$
    ```
  - GitHub Actions에서도 워크플로우에서 그냥 pre-commit을 돌려버리라고 했다
    ```yaml
    - uses: actions/checkout@v4

    - uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - uses: pre-commit/action@v3.0.1
      with:
        extra_args: --all-files
    ```
  - 문제는 우리 회사가 GitHub Enterprise에서 pre-commit/action를 제공 안 하고 있었고, 프록시 서버를 쓰고 있었기 때문에 삽질을 좀 해줬다
    - 프록시 관련 환경변수를 추가해줬는데
    - HTTP_PROXY, http_proxy는 GitHub Actions가 중복이라고 하는 바람에 대문자 환경변수만 추가해줬다
    - 그리고 사내 GitHub는 프록시를 타면 안 되기 때문에 NO_PROXY도 지정해줬다
    - 그래서 결과적으로 이런 모양이 됨
      ```yaml
      name: Python code style checker with isort and black

      on:
        pull_request:
          branches: ["dev"]

      jobs:
        python-lint-checker:
          runs-on: [self-hosted]

          env:
            HTTP_PROXY: http://proxy.company.com:8080
            HTTPS_PROXY: http://proxy.company.com:8080
            NO_PROXY: localhost,127.0.0.1,.company.com

          steps:
            - uses: actions/checkout@v3

            - uses: actions/setup-python@v4
              with:
                python-version: "3.12"

            - name: Install pre-commit
              run: |
                python -m pip install --upgrade pip
                python -m pip install pre-commit

            - name: Run pre-commit
              run: |
                pre-commit run --all-files
      ```
  - 굳이 GitHub Actions에서 linter를 직접 돌리지 않고 pre-commit으로 해결해버려도 된다는 걸 알은 건 너무 큰 소득이었다!

## 2026-06-12

- [Conventional Commits](https://www.conventionalcommits.org/ko/v1.0.0/)
  - 팀에서는 자체적인 커밋 메시지 및 PR 메시지 작성 규칙을 사용해오고 있었는데, Claude Code나 OpenCode의 사용이 본격화 되면서부터 언제부턴가 `feat: add new something blahblah` 같이 자체 작성 규칙이 아닌 형태의 커밋과 PR이 남발하기 시작해서 의아해하고 있었다.
  - 근데 알고 봤더니 Conventional Commits이라 해서 단순히 개발자들이 쉽게 이해하는 메시지를 넘어 machine readable함까지 추가한 일종의 스펙같은 게 있다는 것이었다.
  - 원래는 Angular 쪽에서 사용하던 규약이라고 하고.
  - 저걸 좀 일찍 알았으면 적용했을까 싶기도 한데 CHANGELOG는 활용하고 있지 않아서 일단은 팀에서 하고 있는 규칙 그대로 밀고 나가도 될 것 같긴 하다.